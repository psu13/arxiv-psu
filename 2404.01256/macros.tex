% Use this for defined terms
\newcommand{\defemph}[1]{\textnormal{\emph{\textbf{#1}}}}
\newcommand{\defeq}{\coloneq}
\newcommand{\defiff}{\mathrel{{\mathop:}{\Leftrightarrow}}}

% Pullback corners
\newcommand{\pullbackcorner}[1][ul]{\save*!/#1+1.2pc/#1:(1,-1)@^{|-}\restore}

% Quantifiers
\newcommand{\all}[1]{\forall #1 .\,}
\newcommand{\some}[1]{\exists #1 .\,}


\newcommand{\limply}{\to} % Implication in the tripos
\newcommand{\lthen}{\Rightarrow} % Implication in the topos
\newcommand{\liff}{\Leftrightarrow} % shoter iff

\newcommand{\of}{{:}} % colon as written in a typing context

% pca abstraction
\newcommand{\abstr}[1]{\langle#1\rangle \,}

% set comprehension { x | p } is written as \set{ x \such p }
\newcommand{\set}[1]{\{#1\}}
\newcommand{\such}{\mid}

\newcommand{\carrier}[1]{|#1|} % the carrier set of an assembly or an object

\newcommand{\pair}[1]{\langle #1 \rangle} % a pairing function

\newcommand{\parto}{\rightharpoonup} % partial maps
\newcommand{\multito}{\rightrightarrows} % multi-valued map

\newcommand{\pow}[1]{\mathcal{P}(#1)} % powerset
\newcommand{\powcl}[1]{\mathcal{P}_{\neg\neg}(#1)} % classical powerset
\newcommand{\powinh}[1]{\mathcal{P}_{*}(#1)} % inhabited powerset

\newcommand{\Conv}[1]{\mathrm{Conv}(#1)} % non-empty convex compact subsets of Hilbert cube

\newcommand{\id}[1][]{\mathrm{id}_{#1}} % identity map

\newcommand{\Hom}[1]{\mathrm{Hom}(#1)} % the Hom functor

% black-board letters
\renewcommand{\AA}{\mathbb{A}}  % the carrier of a ppca
\newcommand{\PP}{\mathbb{P}}  % the parameter set of a ppca
\newcommand{\NN}{{\mathbb{N}}}  % natural numbers (for some reason we need extra {...} here, or LaTeX whines)
\newcommand{\ZZ}{\mathbb{Z}}  % integers
\newcommand{\QQ}{\mathbb{Q}}  % rational numbers
\newcommand{\HH}{\mathbb{H}}  % Hilbert cube
\newcommand{\II}{\mathbb{I}}  % the interval domain

\newcommand{\KK}[1][]{\mathbb{K}^{#1}} % Kleene's first algebra w.r.t to optional oracle

\newcommand{\RRc}{\mathsf{R}_\mathsf{c}}  % Cauchy real numbers object
\newcommand{\RRd}{\mathsf{R}}  % Dedekind real numbers object
\newcommand{\RRcl}{\mathsf{R}_{\neg\neg}}  % the classical real numbers object
\newcommand{\RR}{\mathbb{R}}  % the real real numbers

\newcommand{\Cut}[1]{\mathsf{cut}(#1)} % Dedekind cut
\newcommand{\ClCut}[1]{\mathsf{cut}_{\neg\neg}(#1)} % classical Dedekind cut

% representation of a real by an oracle
\newcommand{\rrep}{\mathcal{r}}

% representation of a real sequence by an oracle
\newcommand{\srep}{\mathcal{s}}

% double complement
\newcommand{\compl}[1]{#1^{\mathsf{c}}} % complement
\newcommand{\dcompl}[1]{#1^{\mathsf{cc}}} % double complement

\newcommand{\one}{\mathsf{1}} % the terminal object
\newcommand{\two}{\mathsf{2}} % the booleans 2 = {0, 1}
\newcommand{\Cantor}{\two^\NN} % the Cantor space

\newcommand{\ClProp}{\Omega_{\neg\neg}} % classical truth values

\newcommand{\mil}{\upmu} % a Miller sequence
\newcommand{\MM}[1]{\mathbb{M}_{#1}}  % the parameter for Miller oracle
\newcommand{\TT}[1]{\mathcal{T}_{#1}} % the topos with countable reals


% PPCAs
\newcommand{\R}[1]{\mathtt{#1}} % a font for realizers

\newcommand{\comb}[1]{\R{#1}} % a combinator
\newcommand{\combK}{\comb{k}} % the K combinator
\newcommand{\combS}{\comb{s}} % the S combinator

\newcommand{\numeral}[1]{\overline{#1}} % the Curry numeral

\newcommand{\combCur}{\comb{cur}} % realizer converting a number to a Curry numeral
\newcommand{\combNum}{\comb{num}} % realizer converting a Curry numeral to a number


\newcommand{\combFst}{\comb{fst}} % the first projection
\newcommand{\combSnd}{\comb{snd}} % the second projection
\newcommand{\combPair}{\comb{pair}} % the pairing
\newcommand{\combTrue}{\comb{true}} % true
\newcommand{\combFalse}{\comb{false}} % false
\newcommand{\combIf}{\comb{if}} % conditional

\newcommand{\appsymbol}{{\cdot}} % the name of application
\newcommand{\app}[1][]{\cdot_{#1}} % application
\newcommand{\defined}[1]{#1\,{\downarrow}}
\newcommand{\kleq}{\simeq} % Kleene equality

\newcommand{\at}{\shortmid}

\newcommand{\att}{\shortparallel} % used to construct a ppca from a family of pcas

\newcommand{\rz}[1][]{\Vdash_{#1}} % the tripos realizability relation
\newcommand{\Ex}[1]{\mathsf{E}_{#1}} % the existence predicate

\newcommand{\objN}{\mathsf{N}} % the natural numbers object
\newcommand{\objZ}{\mathsf{Z}} % the object of integers
\newcommand{\objQ}{\mathsf{Q}} % the object of rationals
\newcommand{\objI}{\mathsf{I}} % the closed unit interval object

\newcommand{\invim}[1]{#1^{*}} % the inverse image

% Partial computable maps
\newcommand{\pr}[2][]{\upvarphi^{#1}_{#2}}

% An a-computable real, where a is an element of the Hilbert cube
\newcommand{\rcomp}[2]{\Upphi^{#1}_{#2}}

\newcommand{\prx}[3][]{\upvarphi^{#1}_{#2}{\restriction}_{#3}} % a finite paproximation of a parital computable map

\newcommand{\rat}[1]{\mathsf{q}_{#1}} % A bijection ℕ → ℚ ∩ [0,1]

\newcommand{\ucode}[1]{\ulcorner #1 \urcorner} % The uniform code of a realizer

\newcommand{\nat}[1]{\widehat{#1}} % Coding of a number as an oracle
\newcommand{\seq}[1]{\widehat{#1}} % Coding of a sequence as an oracle

\newcommand{\Set}{\mathsf{Set}} % the category of sets and maps
\newcommand{\Heyt}{\mathsf{Heyt}} % the category of Heyting prealgebras
\newcommand{\op}[1]{#1^\mathsf{op}} % the opposite category


% The tripos predicates
\newcommand{\PredSymbol}[1][]{\mathsf{Pred}_{#1}}
\newcommand{\Pred}[2][]{\PredSymbol[#1](#2)}

\newcommand{\PRT}[1]{\mathsf{PRT}(#1)} % parameterized realizability topos
\newcommand{\PAsm}[1]{\mathsf{PAsm}(#1)} % parameterized assemblies

\newcommand{\eq}[1][]{\sim_{#1}} % equality for a topos object

\newcommand{\hull}{\mathsf{hull}} % Convex hull operator

\newcommand{\JComment}[1]{\textcolor{blue}{#1}} % Comments by James.
\newcommand{\AComment}[1]{\textcolor{magenta}{#1}} % Comments by Andrej.


%%% Local Variables:
%%% mode: latex
%%% TeX-master: "countable-reals"
%%% End:
