% Сгрузил с интернета способ метить footnotes своим набором символов.
% см. конец файла. Слегка переделанный вариант реализован здесь

% 1. Не работает комбинация \footnotemark, \footnotetext{...} в \begin{alignat} ...
% Решение: \footnote{} ... \footnotetext{текст}. Т.е.
% один "избыточный" \footnote в align обрабытывается как \footnotemark

% 2. При "неудачном" сочетании footnote's ссылка может перейти на другую стр
% При таком стечении обстоятельств сильно затормозится (3-4раза) TeXify|PDFTeXify-компиляция
% Если исправить, то после нормализации ссылок на свои страницы все становится Ok.
% !Этого "эффекта" нет для 1-кратных компиляций LaTeX, PDFLaTeX. Работает все быстро.

% Сноски на каждой странице отдельно; Гуссенс, стр.90
\RequirePackage{footnpag} % сбрасывает счетчик на каждой стр.

\RequirePackage{color}
\DeclareOption{red}{\def\FColor{\color[rgb]{1,0,0}}}
\DeclareOption{blue}{\def\FColor{\color[rgb]{0,0,1}}}
\DeclareOption{cyan}{\def\FColor{\color[rgb]{0,1,1}}}
\DeclareOption{green}{\def\FColor{\color[rgb]{0,0.6,0}}}
\DeclareOption{yellow}{\def\FColor{\color[rgb]{1,1,0}}}
\DeclareOption{magenta}{\def\FColor{\color[rgb]{1,0,1}}}

% Default-цвет меток: dvi -> красный, pdf -> yellow
\ifx\pdfoutput\undefined\ExecuteOptions{red}%
\else\ExecuteOptions{yellow}\fi
\ProcessOptions

% Увеличить длину линейки 5pc -> 8pc; взято из amsart
\def\footnoterule{\kern-.4\p@
	\hrule\@width 8pc\kern11\p@\kern-\footnotesep}

\newdimen{\@Fpar} \newdimen{\@fWidth}%
\@Fpar=1em % ширина у всех \foonotemark одинакова
\@fWidth=\textwidth%
\addtolength{\@fWidth}{-\@Fpar}

% Команда \@makefntext создает текст \footnote. Львовский, стр.336
% Она переработана из ...tex/latex/base/book.cls
% делает выравнивание значков ** вправо (в amsart.cls - влево)
\renewcommand\@makefntext[1]{\noindent\hb@xt@\@Fpar{\hss\@makefnmark\,\,}%
	\parbox[t]{\@fWidth}{\parindent=1.5em\noindent#1}}

\RequirePackage{amsbsy}% простенький ams-пакет для \boldsymbol
\renewcommand{\thefootnote}{$\FColor\boldsymbol{\mysymb{footnote}}$}

\def\mysymb#1{{\@mysymb{\@nameuse{c@#1}}}}
% Команда ниже чувствительна к ШИРИНЕ(!) символа footnotemark
\def\@mysymb#1{\ifcase#1\or*\or{*}{*}\or{*}{*}{*}\or{*}{*}{*}{*}
\or\circledcirc\or\divideontimes\else\@ctrerr\fi}

% прежняя версия \ldots со случаями непредсказуемых сбоев
% т.е. когда меняется форматирвание всего tex-фокумента
% и компиляция тормозится в 4-5 раз!
%\def\@mysymb#1{\ifcase#1\or *\or*\!*\or{*\!*}\!*
%\or{*\!*}\!{*\!*}\or{*\!*}\!{*\!*}\!*\else\@ctrerr\fi}

% пробовал пакет footmisc. Такая же катавасия на большом файле
% с возможным непредсказуемыми сбоями в сносках

\endinput

Hi-

Many thanks to everybody who sent help.  Here is a summary of what they
said:

1.  Use the following line either before or after the
\begin{document} line:

    \renewcommand{\thefootnote}{\fnsymbol{footnote}}

    or

    \def\thefootnote{\fnsymbol{footnote}}

    in the preamble.

2.  Georgios Dimitriadis at the Univ of Crete-Heraklion said that you
    can use your own sequence of symbols by defining

    \def\mysymbols#1{\@mysymbols{\@nameuse{c@#1}}}
    \def\@mysymbols#1{\ifcase#1\or a\or b\or c\or d\else\@ctrerr\fi}

    Then you can say

    \def\thefootnote{\mysymbols{footnote}}

    where in the above case 'mysymbols' are a,b,c and d but you can
    insert whatever you like.

There were several others, but I stopped when the \renewcommand in
number 1 above worked.

Stefan Gerberding's posting also looked good and I'm going to try it.

Thanks again for the help.

--Robert
